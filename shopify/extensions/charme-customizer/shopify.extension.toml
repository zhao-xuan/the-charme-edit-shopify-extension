name = "charme-customizer"
type = "theme"

# Theme app extension that renders the Charmé charm customizer as an app block
# merchants can drop onto any product page from the theme editor.

[[extensions]]
name = "Charmé Customizer"
handle = "charme-customizer"
type = "theme"
